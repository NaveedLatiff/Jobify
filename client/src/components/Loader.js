const Loader = () => {
  return (
    <div className="min-h-screen w-full fixed inset-0 z-50 flex items-center justify-center bg-black">
      <div className="w-10 h-10 border-3 border-white/10 border-t-white rounded-full animate-spin" />
    </div>
  );
};

export default Loader;